import { Filters } from './Filters.jsx'

export const Header = () => {
    return(
        <header className='header'>
            <h1>React Shop</h1>
            <Filters />
        </header>
    )
}